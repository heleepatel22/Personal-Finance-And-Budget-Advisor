# Personal Finance & Budget Advisor — Python dependencies
# Install with: pip install -r requirements.txt
#
# Versions below marked [verified] were checked against the actual installed
# environment during development. Versions marked [minimum] are expressed as
# floors (package>=X) rather than exact pins, since exact upstream releases
# could not be verified against PyPI in the sandbox this project was authored
# in (no outbound network access). Run `pip freeze > requirements.txt` after
# your first successful install to lock exact versions for your environment.

# --- Core web framework [verified] -------------------------------------------
Flask==3.1.3
Werkzeug==3.1.7
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.1
blinker==1.9.0
MarkupSafe==3.0.3

# --- Database / ORM [minimum] -------------------------------------------------
Flask-SQLAlchemy>=3.1.1
SQLAlchemy>=2.0.30
PyMySQL>=1.1.0
Flask-Migrate>=4.0.5

# --- Forms / auth / security [minimum] ----------------------------------------
Flask-WTF>=1.2.1
WTForms>=3.1.2
Flask-Login>=0.6.3
Flask-Mail>=0.9.1

# --- Data analysis & machine learning [minimum] --------------------------------
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.2

# --- Reports [minimum] -----------------------------------------------------------
reportlab>=4.2.0
openpyxl>=3.1.2

# --- Deployment [minimum] ---------------------------------------------------------
gunicorn>=22.0.0
